import java.util.ArrayList;
import java.util.Scanner;
/**
 * Write a description of class StringCheck here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class StringCheck
{
    ArrayList<strings>words = new ArrayList<String>();
    public ArrayList<String>wordChecker(String[] str, string key){
       
    }
    public String[] wordChecker[ArrayList<Stringstr,StringKey){
        
    }
    
    public int sampleMethod(int y)
    {
      String[] newarray = new String[]
      return newarray;
        
    }
    public static void main(){
    StringChecker sc = new StringChecker    
    }
    
    public StringCheck(){
        Scanner kb = new Scanner(System.in);
        //add key word
        System.out.println("please enter Key Word"):
        keyWord = kb nextline();
        if(!inputStr.equals("quit")){
            words.add(inputStr);
        }
        
        
    public void getInput()}
     Scanner kb = new Scanner(System.in);
        String inputStr = "";
        while(!inputStr.equals("quit")){
            inputStr = kb.nextline();
            words.add(inputStr);
        }
        
        public String[] extentArray(String[] p, int x){
            String[] newarray = New String[p.length+x];
            newarray[i] = p[i];
            
    }
}

    
